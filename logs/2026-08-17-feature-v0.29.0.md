# v0.29.0 — Logo/banner + vista previa en el onboarding

**Fecha**: 2026-08-17
**Tipo**: feature
**Autor**: Clr. Ari Mangini (asistido por DeepSeek V4 Pro — GitHub Copilot)

---

## CONTEXTO

Siguiente paso acordado: logo/banner + previsualización en el onboarding de organizaciones
(paso 1 de la lista). Permite elegir el banner (degradés curados) y el color de acento con
vista previa en vivo mientras se completa el formulario.

## PLAN

1. Migración `spec/init_v0.29.0_empresas_apariencia.sql` (columnas `logo_url`, `cover_url`,
   `cover_gradient`).
2. Recrear `app/empresas/crear/page.tsx` con estado controlado, selector de banner, acento y
   vista previa en vivo.
3. `app/e/[slug]/page.tsx` usa `cover_gradient` en el banner.

## EJECUCIÓN

| Archivo | Estado |
|---|---|
| `spec/init_v0.29.0_empresas_apariencia.sql` | ✅ CREADO |
| `app/empresas/crear/page.tsx` | ✅ RECREADO — banner + vista previa |
| `app/e/[slug]/page.tsx` | ✅ MODIFICADO — cover_gradient |

## PROBLEMAS

Sin bloqueos.

## DECISIONES

- Logo por iniciales + color de acento (la subida de imagen a Storage queda para un próximo
  paso, como indica el texto de la UI).
- Banner con 8 degradés curados (paleta de Espacio Crítico).

## QA

- [x] `npx tsc --noEmit` limpio
- [x] `npm run build` OK
- [x] Vista previa reactiva a nombre/tagline/acento/banner
