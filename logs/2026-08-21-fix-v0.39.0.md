# v0.39.0 — Correcciones pedidas: topbar de antes + sidebar de captura, Jitsi 1-1 profesional, link de video en turnos, campus con acceso independiente

**Fecha**: 2026-08-21
**Tipo**: fix + feature
**Autor**: Clr. Ari Mangini (asistido por DeepSeek V4 Pro — GitHub Copilot)

---

## CONTEXTO

El usuario no aprobó el rediseño v0.38 (quedó distinto a la captura y se removieron cosas que no
pedía). Pide:
1. Restaurar la **barra horizontal superior como estaba antes** (con el botón Campus arriba a la
   derecha, destacado) + el **menú vertical izquierdo de la captura** + KPIs rectangulares grandes.
2. Las videollamadas del perfil profesional con Daily.co no andan (piden tarjeta) → pasarlas a
   **Jitsi Meet 1-1**.
3. Link de video configurable por espacio, enviado SIEMPRE con el recordatorio de WhatsApp y
   disponible al lado del turno del día para acceso rápido.
4. Campus: las videoconferencias piden anfitrión → organizarlo; los espacios se van a alquilar por
   separado (talleres/cursos/workshops/masterclass) → cada espacio debe accederse de forma
   independiente.

## EJECUCIÓN

| Archivo | Estado |
|---|---|
| `app/(empresa)/empresa/page.tsx` | ✅ RECREADO — topbar de antes + sidebar captura + KPIs + 6 pestañas |
| `app/(counselor)/panel/sala-1-1/page.tsx` | ✅ RECREADO — Jitsi 1-1 (sala determinística por profesional) |
| `app/api/daily/room/route.ts` | ❌ ELIMINADO (sin Daily) |
| `package.json` | sin `@daily-co/daily-js` |
| `spec/init_v0.39.0_consultorio_video.sql` | ✅ NUEVO — columna `users.jitsi_base` |
| `lib/video.ts` | ✅ NUEVO — `roomUrl()` por turno |
| `app/(counselor)/panel/consultorio/agenda/page.tsx` | ✅ MODIFICADO — jitsi_base + props |
| `app/(counselor)/panel/consultorio/agenda/AgendaClient.tsx` | ✅ MODIFICADO — 🎥 al lado de cada turno + WhatsApp con link |
| `app/(counselor)/panel/consultorio/agenda/[id]/page.tsx` | ✅ MODIFICADO — 🎥 Iniciar + WhatsApp con link |
| `app/(counselor)/panel/consultorio/ajustes/page.tsx` | ✅ MODIFICADO — configurar base Jitsi del espacio |
| `app/sala/[room]/page.tsx` | ✅ NUEVO — acceso público e independiente por espacio (Jitsi) |
| `app/(empresa)/empresa/campus/page.tsx` | ✅ MODIFICADO — Jitsi-only, 🔗 link del espacio, anfitrión 1° en ingresar |

## QA

- [x] `npx tsc --noEmit` limpio (tras limpiar `.next`)
- [x] `npm run build` OK (ruta `/sala/[room]`, sala-1-1 ~1.8 kB, agenda con video)

**Pendiente del usuario**: ejecutar `spec/init_v0.39.0_consultorio_video.sql` en Supabase.

**Seguimiento (mismo día)**: con aprobación del usuario, se migró el flujo viejo de `sesiones` pagadas
de Daily.co a Jitsi. Se eliminó `lib/daily.ts`; `api/pagos` ahora genera la sala determinística
(`jitsi_base` + `/newen-sesion-<id>`) y la guarda en `daily_room_url` (nombre histórico, sin
migración). El pago no cambia; solo se simplifica (sin API externa ni tarjeta). `VideoRoom` y las
pantallas `/sesion/[id]` siguen iguales (reciben la URL y la embeben en un iframe).
