# LOG DE DESARROLLO — v0.24.0
# Selector de rol: una misma cuenta admin + profesional
# Fecha: 2026-08-17
# Ejecutado por: Coordinador General (Clr. Ari Mangini)
# Herramienta: DeepSeek V4 Pro vía GitHub Copilot (VS Code)

---

## CONTEXTO

El usuario ingresa al panel de admin con la misma cuenta que su perfil profesional
(counselor). Necesita:

1. Elegir, una vez logueado, si continuar como **admin** o como **profesional**.
2. Logout automático tras **10 minutos de inactividad** (ya existía `useAutoLogout`,
   se confirma cobertura en ambos shells y en la pantalla de elección).

---

## PLAN

| Archivo | Acción |
|---|---|
| `spec/init_v0.24.0_rol_selector.sql` | NUEVO — columna `users.es_admin` + backfill |
| `middleware.ts` | MODIFICAR — detección de capacidades dual + redirect a `/elegir-rol` |
| `app/auth/callback/route.ts` | MODIFICAR — redirect post-OAuth a `/elegir-rol` si es dual |
| `hooks/useEsMultiRol.ts` | NUEVO — hook de capacidades admin/counselor |
| `app/elegir-rol/page.tsx` | NUEVO — pantalla de elección de rol |
| `app/(admin)/layout.tsx` | MODIFICAR — switch "Profesional" (solo cuenta dual) |
| `app/(counselor)/layout.tsx` | MODIFICAR — switch "Admin" (solo cuenta dual) |
| `CHANGELOG.md` | MODIFICAR — entrada v0.24.0 |
| `logs/CHANGELOG_DEV.md` | MODIFICAR — entrada 2026-08-17 |
| `logs/2026-08-17-feature-v0.24.0.md` | NUEVO — este log |

---

## EJECUCIÓN

| Archivo | Estado | Notas |
|---|---|---|
| `spec/init_v0.24.0_rol_selector.sql` | ✅ | ALTER users + backfill es_admin |
| `middleware.ts` | ✅ | select("*") tolerante a columna ausente |
| `app/auth/callback/route.ts` | ✅ | misma lógica de detección dual |
| `hooks/useEsMultiRol.ts` | ✅ | users + counselors (maybeSingle) |
| `app/elegir-rol/page.tsx` | ✅ | chooser con auto-logout |
| `app/(admin)/layout.tsx` | ✅ | botón "Profesional" condicional |
| `app/(counselor)/layout.tsx` | ✅ | botón "Admin" condicional |
| `CHANGELOG.md` | ✅ | entrada v0.24.0 |
| `logs/CHANGELOG_DEV.md` | ✅ | entrada 2026-08-17 |
| `logs/2026-08-17-feature-v0.24.0.md` | ✅ | este log |

---

## PROBLEMAS

1. **Login email/password no pasaba por el selector** — el redirect del login page
   usaba solo `rol` y enviaba directo a /admin o /panel. Corregido: ahora redirige a `/`
   y el middleware decide (incluido `/elegir-rol` para cuentas duales).

2. **Trigger permitía auto-admin vía metadata** — `handle_new_user` copiaba
   `raw_user_meta_data->>'rol'` sin validar. Corregido: queda limitado a 'consultante'
   o 'counselor'; 'admin' solo se asigna manualmente en SQL.

3. **Detección de counselor dependía de RLS** — el chequeo de la fila en `counselors`
   solo ve perfiles `activo`, por lo que el selector podía no aparecer. Corregido:
   detección determinística `esCounselor = rol = 'counselor'` y migración que marca la
   cuenta única con `rol='counselor' + es_admin=true`.

---

## DECISIONES

- **DD: Modelo de capacidades** — `es_admin` (flag) separa la capacidad admin del
  rol primario. `esCounselor` se deduce de `rol='counselor'` o de la existencia de
  fila en `public.counselors`. Cuenta dual = ambas verdaderas.
- **DD: Tolerancia a migración pendiente** — el middleware y el callback usan
  `select("*")` para que, si `es_admin` aún no existe, el fallback a `rol='admin'`
  siga funcionando (evita lockout entre deploy y ejecución del SQL).
- **DD: Auto-logout** — reutiliza `useAutoLogout` (10 min, eventos mousedown/keydown/
  scroll/touchstart) ya presente en ambos layouts; se agrega a `/elegir-rol`.
- **DD: Admin solo manual** — el alta pública nunca crea admin. La marca `es_admin` y
  el rol 'admin' se asignan únicamente desde el SQL Editor.

---

## QA

- [x] `npx tsc --noEmit` — sin errores.
- [x] `npm run build` — éxito. Nueva ruta `/elegir-rol` presente.
- [ ] Pendiente de runtime: ejecutar `spec/init_v0.24.0_rol_selector.sql` en Supabase
      y, si corresponde, marcar la cuenta profesional con `es_admin = true`.
